name: dakar_cafe_express
description: Application marketplace coffee"
authors:
  - "Cheikh Assane THIAM"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.2
  jwt_decoder: ^1.0.4
  splashscreen: ^1.3.5
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.8.0

flutter_icons:
  image_path: "assets/logoa.png"
  android: true
  ios: true

flutter:
  uses-material-design: true
  assets:
    - assets/

